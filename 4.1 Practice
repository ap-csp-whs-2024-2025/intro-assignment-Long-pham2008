int main()
{ 
  int num1 = 5;
  bool isRaining = false;
  std::cout << (num1 > 0) && isRaining << std::endl;
  std::cout << (num1 > 0) || isRaining << std::endl;
  std::cout << !isRaining;
  return 0;
}
